#![allow(unused)]


fn cargo() {
	// - `rustc` - Compile a Rust source file
	// - `cargo new <project-name>` - Create a new Cargo project with the given name
	// - `cargo build` - Build a Cargo project
	// - `cargo run` - Build and run a Cargo project
	// - `cargo check` - Check if a Cargo project compiles without building it
	// - `cargo update` - Update dependencies of a Cargo project
	// - `cargo test` - Run the tests of a Cargo project
	// - `cargo clean` - Clean the build artifacts of a Cargo project
}
